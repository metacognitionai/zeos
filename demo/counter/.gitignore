# Weights: ~2.7GB, fetched with `uv run python demo/counter/fetch_model.py`.
# No trailing slash: during development this is often a symlink to a shared
# download, and `models/` would not match one.
/models
